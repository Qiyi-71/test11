# test11

A clean Next.js starter ready to deploy on Vercel.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Deploy

Push this repository to GitHub, then import it in Vercel. Vercel will detect
Next.js automatically and use the included build settings.
